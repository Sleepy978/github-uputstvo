
git config --global user.name
git config --global user.email

git init ime foldera -  pravis folder tj repozitori
git init ulazis i inicijalizujes ga


git add  - stavljas ih u stagging area
git status   ...vidis stanje
git commit -a  -m     sve commituje sa messagom

git diff
git diff --stage

git rm   file1.txt  priprema za brisanje
git add -u .   priprema za brisanje

git mv   trenutna lokacija  + nova lokacija     pomeranje fajla

git branch  ime_grane
git checkout ime grane

git remote add origin http,,,, -  to pravis sa http onim linkom ili sa SSH

git remote -v   gledas da li je tu origin git remote add origin

staging area  ... u nju se stavljau fajlovi sa git add
commiting






git clone - uzimas foldere i pravis ih na svom kompu
